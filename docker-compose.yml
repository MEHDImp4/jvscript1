services:
  app:
    image: ghcr.io/mehdimp4/jvscript1:latest
    container_name: profile-app
    restart: unless-stopped
    ports:
      - "8080:80"  # host:container
    # This project now uses a local SQLite database (`database.sqlite`).
    # No DB service is required; the app will create `database.sqlite` on first run.

